# ZEN DIVE Manila — プロジェクトルール

マニラ発ダイビングスクールのランディングページ + Gemini チャットアシスタント。
静的サイト（`index.html` / `css` / `js`）+ Vercel Serverless Function（`api/chat.js`）。

背景知識・過去の決定・踏んだ罠は **`Learning.md`** にある（セッション開始時に自動で読み込まれる）。
ここには「毎回守る振る舞い」だけを書く。

## 完了の定義

- **`./scripts/verify.sh` を通してから完了と言う。** 通していないなら「未検証」と明記する。
- フロントを触ったら**ブラウザで開いて console error 0 を目視**する。
  ビルドやサーバ起動が通ったことは、動作の証明ではない。
- チャットを触ったら、実際に1往復させて応答を確認する。
- **「ローカルで直った」と「ユーザーに見える」は別の完了条件。** 見た目・配線を直した後、
  ユーザーに「見れます」と言う前に **`./scripts/check-live.sh` を実行し、本番URLと同期しているか確認する**。
  ずれていたら、push + `vercel --prod`（要人間承認）でデプロイしてから改めて確認する。
  ローカルの `git commit` だけでは本番には一切反映されない。
  （実例: 2026-08-22、フッターロゴの比率修正をローカルでは正しく直し `getBoundingClientRect()` で数値検証までしたが、
  デプロイし忘れて本番は旧CSSのまま → ユーザーが「全然違う」と本番で確認し手戻りになった。`Learning.md` 参照）

## 英語版を触ったら

英語版は日本語版の直訳ではない。**「Japanese」は言語ではなく基準を指す**（`Learning.md` 参照）。
`tests/english-copy.spec.mjs` が以下を機械的に守る:

- 「日本語で教わる」系の売り文句が英語ページに出ないこと
- **未確認の英語対応を約束しないこと**（確認が取れたらテストを更新してから書く）
- **未翻訳の日本語が英語ページに出ないこと** — i18n は JP 原文をキーにするので、
  エントリを書き忘れた文字列は黙って日本語のまま出る（`5:00頃` がそれで本番に出た）
- 意図的に残す日本語（ローディング画面・装飾の「潜降」・二言語キャプション・チャットUI）は
  テストの `INTENTIONAL_JP` に理由付きで登録する

## 見た目を変えたら

見た目の合否は**主観で決めない**。`./scripts/verify.sh` の `[6/6] appearance` が判定する。

- ロゴ・レイアウトの形は `tests/logo-lockup.spec.mjs` が**実描画のインク幅**で検証する。
  要素のボックス幅で判断しない — 232.9 と 232.8 で「一致」と誤判定し、実際の文字幅は
  112px と 231px だった前例がある
- 色・線幅は `tests/__screenshots__/` のピクセル基準画像が守る
- **基準画像の更新（`--update-snapshots`）は、意図した変更を人間が承認する時だけ。**
  テストを通すために更新するのは、テストを消すのと同じ
- 検証を足したら `./scripts/self-test.sh` にミューテーションを追加する。
  「落ちること」を確認していない検証は信用しない

## 触ってはいけないもの

`scripts/verify.sh` と `api/system-prompt.txt` は protected（`.claude/loop/harness.json`）。
検証が厳しすぎる・プロンプトが邪魔だと感じても、**緩めずに報告して止まる**。

## 承認が要る操作

`git push` / `gh pr merge` / `vercel --prod` / `vercel env rm` は、
ループの中であっても人間の承認を取ってから。

## 数字と文言

- **価格・日程・所要時間を推測で書かない。** サイト本文もチャットも同じ。
  不明なら書かないか、確認する。原価と催行条件の根拠は `Learning.md` にある。
- チャットは `api/system-prompt.txt` に無いことを答えない。プロンプトの緩和は都度承認。

## デザインの差し戻しが来たら

案を出し直す前に、**実在のリファレンスを集める**。自分の中から出した案は通らなかった実績がある。
判断基準は `Learning.md`「ユーザーの判断基準」を参照。

## 秘密情報

Gemini のキーは Vercel の環境変数のみ。コード・`wrangler.toml` 相当・コミットに一切書かない。
`.env*` と `.vercel` はコミットしない（`verify.sh` が見張っている）。
