# Learning — zen-dive-manila

セッションをまたいで持ち越す教訓。**次のセッションの自分が読んで得をすることだけ**書く。
コードを読めば分かること・git log に残ることは書かない。

読み込み: セッション開始時（SessionStart フックが自動注入 / `learning-log read`）
書き込み: 作業の区切り（`learning-log save`）
統合整理: 週1回（`learning-log consolidate`）

---

## 原則（consolidate で抽出された、繰り返し効く判断）

- **外部APIの識別子（モデル名・エンドポイント）を知識で書かない。** 実際に一覧を叩いて確認する。
  ここで2回外した（2026-08-19）。
- **デザイン案を出す前に、実在のリファレンスを集める。** 自分の中から出した案は6回連続で却下された。

---

## このプロジェクトの罠

- **Gemini のモデル名** — `gemini-2.5-flash` は "no longer available to new users"。
  現行は `gemini-3.6-flash`。`generativelanguage.googleapis.com/v1beta/models` を叩けば確定する。(2026-08-19)
- **Vercel の環境変数名は `geminiapi`**（`GEMINI_API_KEY` ではない）。`api/chat.js` は両対応にしてある。
  Shared 変数は **Link to Projects でプロジェクトに紐付けないと注入されない**。空だと本番で undefined。(2026-08-19)
- **`.env.local` / `.vercel` は絶対にコミットしない** — Vercel OIDC トークンを含む。`.gitignore` 済み。
  `scripts/verify.sh` の secrets チェックがこれを見張っている。
- **SP で背景が明るいのは仕様** — `pointer: coarse` で WebGL シェーダーが切れる。
  `main.js` が付ける `has-ocean` クラスの有無でフォールバック背景を出し分けている。
  「SPだけ背景が変わった」を自分のバグと誤認しないこと。(2026-08-19)
- **Playwright と Lenis（スムーススクロール）は相性が悪い** — `window.scrollTo` 直後の計測はズレる。
  10〜16分割の段階スクロール＋各90ms待機＋最後に1.5〜2秒待つ。
- **CSS Grid の列幅は、その列に span する要素にも影響される** — フッターロゴが71pxズレた真因。
  `auto` をやめて `grid-template-columns:2.5rem auto` で1列目を固定して解決。修正2回失敗した。(2026-08-19)

---

## 業務知識・ドメイン

- **原価**（領収書からの積算）: ボート1台 ₱6,000（船5,000＋燃料1,000）/ ジプニー1台 ₱1,750 片道 /
  講習実費 ₱4,000。ボート代は人数でほぼ動かないので、**1名だと取り分が ₱8,630〜13,060 にしかならず成立しない**。
- **「2名から催行」は営業方針ではなく、原価構造から出た結論。** 値引き交渉に応じられない理由がここ。
- 送迎はまとめ手配のため、個別値引きができない。

---

## 確定した決定と、その理由

- **オールインクルーシブ（講習＋宿泊の1人あたり総額）を実装したが、分離型に戻した。**(2026-08-18)
  課金単位が2種類（講習＝人単位 / 部屋＝室単位）で、1つの「1人あたり価格」に押し込むと必ず矛盾が出る。
  実装できるかではなく、**価格が説明可能かどうか**で決めた。
- **LEVEL3（レスキュー）以降は名前だけ載せ、価格・詳細は出さない。**
  Webサイト経由でレスキュー以上を取る人はほとんどおらず、詳細はコンバージョンに繋がらないため。
- **チャットボットは、プロンプトに書かれていないことを一切答えない。**
  禁止: 推測金額 / 価格交渉・値引きの約束 / 医療判断（持病・妊娠・投薬中の潜水可否）/
  キャンセル規定 / 保険 / 他店比較。プロンプト本体は `api/system-prompt.txt`（protected）。
- **予約導線は Instagram DM のみ。** サイト内予約フォームは作らない（現時点の運用判断）。

---

## ユーザーの判断基準（デザインの合否がここで決まる）

- ロゴは **「シャツやステッカーになっている姿を想像できるか」** で判断される。
- 求めているのは「インターナショナルなスタンダード」。**和風モチーフ（漢字・筆・円相・落款）は却下された**。
  ZEN は様式ではなく、余白と静けさとして出す。
- 「本業ITのCTOとして、圧倒的な違いを見せつける」が全体の基準。無難な仕上がりは不合格。
- 却下は短い一言で来る（「ダサい」「微妙」）。**理由を聞き返す前に、まずリファレンスを集め直す**。

---

## 失敗ログ

- **ロゴ6案連続却下**（筆風円相 → 金の落款印 → 円形バッジ → こってり4案 → 構図系 → Vの沈み込み）。
  参考調査（Subsurface / PADI公式 / 日本式ロゴガイド）をしてから出した「極細の欠けた円」で決着。
  → 先にリファレンス。これが原則に昇格した。
- **実在人物の顔をAIで作り変える依頼を断った。** 「信頼を伝える」というセクションの目的と逆行するため。
  代替（別写真 / トリミング / 撮り直し）を出し、ユーザーは別写真を選んだ。同種の依頼には同じ判断をする。
- **`.tier__list--band` の padding が効かなかった** — 後方の `.tier__list{padding:0}` に詳細度で負けていた。
  `.tier__list.tier__list--band` の二重指定で解決。

---

<!--
consolidate 手順（週1回）:
1. 3回以上出てきたパターンを「原則」に昇格し、個別項目は削除
2. 同じことを言っている項目をマージ
3. 解決済み・陳腐化した項目を削除（git履歴に残るので消してよい）
4. 各セクション、多くて10項目に保つ。増えたら原則に畳む
-->
